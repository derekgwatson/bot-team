# Ivy - Buz Inventory & Pricing Manager
# Bot-specific environment variables
# Shared variables (BOT_API_KEY, GOOGLE_CLIENT_ID, etc.) are in /bot-team/.env

# Flask secret key for session signing (REQUIRED)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Browser mode (optional - defaults to headless)
# BUZ_HEADLESS=true

# Debug mode - shows browser window for debugging (optional)
# BUZ_DEBUG=false

# Buz organizations to connect to (comma-separated)
# Storage state files must exist for each org in .secrets/
# BUZ_ORGS=canberra,designer_drapes,tweed
