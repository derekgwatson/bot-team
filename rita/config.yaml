# Rita's Configuration
name: rita
description: "User access manager"
version: "0.1.0"
emoji: "🧾"
personality: Security-conscious and careful, takes permissions seriously

# Dependencies: Other bots that Rita requires to function
dependencies:
  - peter   # Staff directory (lookup staff and their current access)

# Server configuration
server:
  host: "0.0.0.0"
  # port is read from chester/config.yaml via shared/config/ports.py

# Shared organization config
# See ../shared/config/organization.yaml for domain definitions
shared_config: "../shared/config/organization.yaml"

database:
  path: "database/rita.db"

# Authentication configuration

# Authentication configuration
# Uses Chester's auth gateway for Google OAuth
auth:
  mode: domain  # Any user from allowed_domains can access
  allowed_domains:
    - watsonblinds.com.au
    - watsoncommercial.com.au
    - designerdrapes.com.au
