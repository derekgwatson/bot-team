# Bot-specific environment variables for Evelyn
# Shared variables (BOT_API_KEY, GOOGLE_CLIENT_ID, etc.) are in /bot-team/.env

# Admin emails (comma-separated) - optional override
# EVELYN_ADMIN_EMAILS=admin@example.com
