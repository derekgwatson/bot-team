# Banji Environment Configuration
# Copy this to .env and fill in the values

# ── Flask Configuration ────────────────────────────────
FLASK_SECRET_KEY=your-secret-key-here-change-in-production
FLASK_DEBUG=True  # Set to False in production

# ── Google OAuth Configuration ─────────────────────────
# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# are in the shared .env file at the root of bot-team/

# Admin email addresses (comma-separated)
# Only these emails can access Banji's web interface
ADMIN_EMAILS=your@email.com,another@email.com

# ── Buz Application Settings (Multi-Org) ───────────────
# Buz is multi-tenant - different organizations have different auth sessions
# List all organizations you want Banji to support (comma-separated)
BUZ_ORGS=designer_drapes,canberra,tweed

# Authentication uses Playwright storage state files (saved browser sessions)
# NO PASSWORDS stored in .env - more secure!
#
# For each organization listed above, you need a storage state file:
#   .secrets/buz_storage_state_designer_drapes.json
#   .secrets/buz_storage_state_canberra.json
#   .secrets/buz_storage_state_tweed.json
#
# Generate these files by running the bootstrap tool for each org:
#   python tools/buz_auth_bootstrap.py designer_drapes
#   python tools/buz_auth_bootstrap.py canberra
#   python tools/buz_auth_bootstrap.py tweed
#
# The bootstrap tool opens a browser window where you:
#   1. Login to Buz (including MFA if needed)
#   2. Select the correct organization
#   3. Navigate through required areas to capture all auth cookies
#   4. The tool saves the authenticated session to the storage state file
#
# Benefits:
#   - No passwords in .env files
#   - Handles MFA automatically
#   - Works with complex auth flows
#   - Sessions can be revoked/regenerated easily

# ── Browser Configuration ──────────────────────────────
# Browser mode: true for headless, false for headed
# If not set, defaults based on FLASK_DEBUG:
#   - FLASK_DEBUG=True  -> headed (visible browser for development)
#   - FLASK_DEBUG=False -> headless (invisible browser for production)
# BUZ_HEADLESS=false
