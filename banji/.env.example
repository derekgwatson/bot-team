# Banji Environment Configuration
# Copy this to .env and fill in the values

# ── Flask Configuration ────────────────────────────────
FLASK_SECRET_KEY=your-secret-key-here-change-in-production
FLASK_DEBUG=True  # Set to False in production

# ── Bot API Authentication ─────────────────────────────
# Shared API key for bot-to-bot communication
BOT_API_KEY=your-bot-api-key-here

# ── Chester Service Registry ───────────────────────────
# URL to Chester (service registry) for discovering other bots
CHESTER_API_URL=http://localhost:8008

# ── Buz Application Settings ───────────────────────────
# Base URL for Buz application (required)
BUZ_BASE_URL=https://buz.example.com

# Buz login credentials (required)
BUZ_USERNAME=your-buz-username
BUZ_PASSWORD=your-buz-password

# ── Browser Configuration ──────────────────────────────
# Browser mode: true for headless, false for headed
# If not set, defaults based on FLASK_DEBUG:
#   - FLASK_DEBUG=True  -> headed (visible browser for development)
#   - FLASK_DEBUG=False -> headless (invisible browser for production)
# BUZ_HEADLESS=false
