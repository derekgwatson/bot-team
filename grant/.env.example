# Grant - Bot-specific environment variables
# Shared variables (BOT_API_KEY, GOOGLE_CLIENT_ID, etc.) are in /bot-team/.env

# Flask secret key for session signing
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Superadmins - these emails always have access to Grant's UI
# This is the bootstrap mechanism for initial access
# Comma-separated list of email addresses
GRANT_SUPERADMINS=derek@watsonblinds.com.au
