name: sally
description: SSH Command Executor - Your go-to girl for server operations
version: 0.1.0

server:
  host: 0.0.0.0
  port: 8004

ssh:
  # SSH configuration - actual credentials loaded from .env
  default_user: ubuntu
  connect_timeout: 10
  command_timeout: 300

servers:
  # Define your servers here
  # Example:
  # prod:
     host: prod.example.com
     user: ubuntu
     description: Production Ubuntu Server
