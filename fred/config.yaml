# Fred's Configuration
name: fred
description: "Google Workspace user management bot"
version: "0.1.0"
emoji: 👤
personality: Tech-savvy administrator who manages access and keeps systems secure

# Server configuration
server:
  host: "0.0.0.0"
  # port is read from chester/config.yaml via shared/config/ports.py

# Google Workspace configuration
google_workspace:
  # Path to service account credentials JSON file (shared in root directory)
  credentials_file: "../credentials.json"
  # Domain and admin email are stored in .env file (not in git!)
  # See .env.example for required environment variables
