# Fred's Environment Variables
# Copy this file to .env and fill in your values

# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# and BOT_API_KEY are in the shared .env file at the root of bot-team/

# ── Google Workspace API ─────────────────
# Path to service account credentials JSON file
# Development: Store in bot directory (e.g., fred/credentials.json)
# Production: Use dedicated secrets directory (e.g., /var/secrets/bot-team/google-service-account.json)
#             Set permissions: chmod 600, owned by bot process user
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Your Google Workspace customer ID
GOOGLE_CUSTOMER_ID=your-google-customer-id

# Admin email for Google Workspace API delegation
GOOGLE_WORKSPACE_ADMIN_EMAIL=admin@yourdomain.com

# Google Workspace Domain
# This is the primary domain for your Google Workspace
GOOGLE_WORKSPACE_DOMAIN=example.com

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Allowed email addresses (comma-separated)
# Only these emails can access Fred's web interface
ALLOWED_EMAILS=your@email.com,another@email.com
