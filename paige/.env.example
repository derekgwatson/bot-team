# Bot-specific environment variables for Paige
# Shared variables (BOT_API_KEY, GOOGLE_CLIENT_ID, etc.) are in /bot-team/.env

# Flask secret key for session signing (REQUIRED for web UI)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# DokuWiki installation path (optional - defaults to /var/www/dokuwiki)
# DOKUWIKI_PATH=/var/www/dokuwiki

# Admin emails - optional when using Grant mode for authorization
# PAIGE_ADMIN_EMAILS=admin@watsonblinds.com.au
