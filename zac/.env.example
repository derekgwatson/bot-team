# Zac - Zendesk User Management Bot
# Environment Variables Configuration
# Copy this file to .env and fill in your values

# Zendesk Configuration
# Get these from your Zendesk admin panel: Admin > Channels > API
ZENDESK_SUBDOMAIN=yourcompany
ZENDESK_EMAIL=admin@example.com
ZENDESK_API_TOKEN=your-api-token-here

# Google OAuth for login (REQUIRED for security)
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Enable Google+ API and create OAuth 2.0 credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Flask secret key for sessions
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Allowed admin email addresses (comma-separated)
# Only these users can access the web interface and API
ADMIN_EMAILS=your@email.com,another@email.com

# API Key for bot-to-bot communication
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
BOT_API_KEY=your-secret-api-key-here
