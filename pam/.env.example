# Pam Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# Google OAuth for login (REQUIRED for security)
# Get these from Google Cloud Console > APIs & Credentials > OAuth 2.0 Client IDs
# Note: Also supports legacy variable names (GOOGLE_OAUTH_CLIENT_ID, GOOGLE_OAUTH_CLIENT_SECRET)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Allowed email addresses (comma-separated)
# Only these emails can access Pam's web interface
ALLOWED_EMAILS=your@email.com,another@email.com

# API Key for bot-to-bot communication (must match Quinn's key)
BOT_API_KEY=your-secret-api-key-here

# Bot URLs (REQUIRED in production)
# Development: Defaults to localhost:8005 (Quinn) and localhost:8003 (Peter)
# Production: Set these to your actual bot domains
QUINN_API_URL=https://quinn.watsonblinds.com.au
PETER_API_URL=https://peter.watsonblinds.com.au
