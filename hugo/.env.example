# Hugo - Buz User Management Bot
# Bot-specific environment variables
# Shared variables (BOT_API_KEY, GOOGLE_CLIENT_ID, etc.) are in /bot-team/.env

# Flask secret key for session signing (REQUIRED)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Comma-separated list of Buz organizations to manage
# Each org needs a storage state file in .secrets/buz_storage_state_{org}.json
BUZ_ORGS=canberra,tweed,dd,bay,shoalhaven,wagga

# Optional: Override headless mode (default: true in prod)
# BUZ_HEADLESS=true

# Debug mode: enables headed browser + Playwright Inspector pauses
# Set to true to step through automation for troubleshooting
# BUZ_DEBUG=false
