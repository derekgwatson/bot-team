# Sadie - Zendesk Ticket Information Bot
# Environment Variables Configuration
# Copy this file to .env and fill in your values

# Note: Zendesk credentials (ZENDESK_SUBDOMAIN, ZENDESK_EMAIL, ZENDESK_API_TOKEN),
# Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET), and BOT_API_KEY
# are in the shared .env file at the root of bot-team/

# Flask secret key for sessions
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Allowed email domains for all-staff access (comma-separated)
# Anyone with an email from these domains can access Sadie
ALLOWED_DOMAINS=yourcompany.com,example.com
