name: sadie
description: "Zendesk ticket information bot"
version: "0.1.0"
emoji: "🎫"
server:
  host: "0.0.0.0"
  # port is read from chester/config.yaml via shared/config/ports.py
database:
  path: "database/tickets.db"
