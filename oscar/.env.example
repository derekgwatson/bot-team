# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# are in the shared .env file at the root of bot-team/

# Admin emails (comma-separated list of authorized admins)
ADMIN_EMAILS=admin@watsonblinds.com.au,admin2@watsonblinds.com.au

# Email Configuration
# Emails are sent via Mabel (the centralized email bot)
NOTIFICATION_EMAIL=ian@watsonblinds.com.au

# Optional: Mabel URL (defaults to http://localhost:8010)
# MABEL_URL=http://localhost:8010

# Flask Secret Key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
FLASK_SECRET_KEY=your-secret-key-here

# Optional: Enable Flask debug mode (defaults to False for security)
# Set to "true" for development - shows detailed errors and auto-reloads
# FLASK_DEBUG=true
