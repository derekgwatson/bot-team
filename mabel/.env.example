# Mabel Email Bot - Environment Variables
# Copy this file to .env and fill in your actual values

# Flask secret key for session signing (generate a random string)
FLASK_SECRET_KEY=your-secret-key-here-change-me

# Internal API key for inter-bot authentication
# Other bots must include this in X-Internal-Api-Key header
MABEL_INTERNAL_API_KEY=your-internal-api-key-here-change-me

# SMTP credentials for sending emails
EMAIL_SMTP_USERNAME=your-smtp-username@example.com
EMAIL_SMTP_PASSWORD=your-smtp-password-here

# Optional: Override SMTP settings (if not using config.yaml defaults)
# EMAIL_SMTP_HOST=smtp.example.com
# EMAIL_SMTP_PORT=587
