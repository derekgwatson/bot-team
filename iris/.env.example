# Iris's Environment Variables
# Copy this file to .env and fill in your values

# Note: Google Workspace API credentials (GOOGLE_APPLICATION_CREDENTIALS, GOOGLE_CUSTOMER_ID, GOOGLE_ADMIN_EMAIL)
# and Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# are in the shared .env file at the root of bot-team/

# Google Workspace Domain
# This is the primary domain for your Google Workspace
GOOGLE_WORKSPACE_DOMAIN=example.com

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Allowed email addresses (comma-separated)
# Only these emails can access Iris's web interface
ALLOWED_EMAILS=your@email.com,another@email.com
