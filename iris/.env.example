# Iris's Environment Variables
# Copy this file to .env and fill in your values

# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET),
# Google Workspace credentials (GOOGLE_WORKSPACE_ADMIN_EMAIL, GOOGLE_WORKSPACE_DOMAIN),
# and BOT_API_KEY are in the shared .env file at the root of bot-team/

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Optional: Enable Flask debug mode (defaults to False for security)
# Set to "true" for development - shows detailed errors and auto-reloads
# FLASK_DEBUG=true

# Admin email addresses (comma-separated)
# Only these emails can access Iris's web interface
ADMIN_EMAILS=your@email.com,another@email.com
