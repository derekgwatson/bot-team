# Iris's Configuration
name: iris
description: "Google Workspace reporting and analytics bot"
version: "0.1.0"
emoji: 👥
personality: Analytical and insightful, turns data into actionable insights

# Server configuration
server:
  host: "0.0.0.0"
  # port is read from chester/config.yaml via shared/config/ports.py

# Google Workspace configuration
google_workspace:
  # Path to service account credentials JSON file
  credentials_file: "credentials.json"
  # Domain and admin email are stored in .env file (not in git!)
  # See .env.example for required environment variables

# Authentication configuration
# Uses Chester's auth gateway for Google OAuth
auth:
  mode: domain  # Any user from allowed_domains can access
  allowed_domains:
    - watsonblinds.com.au
    - watsoncommercial.com.au
    - designerdrapes.com.au

