# Fred - Google Workspace User Management Bot
# Admin-only interface for managing Google Workspace users

# Initial HTTP-only configuration
# Run certbot to obtain SSL certificates and enable HTTPS:
#   sudo certbot --nginx -d fred.watsonblinds.com.au
# Certbot will automatically add the HTTPS server block and redirect

server {
    listen 80;
    listen [::]:80;
    server_name fred.watsonblinds.com.au;

    # Logging
    access_log /var/log/nginx/fred.access.log;
    error_log /var/log/nginx/fred.error.log;

    # Maintenance mode toggle (touch /var/www/bot-team/fred/maintenance.on)
    if (-f /var/www/bot-team/fred/maintenance.on) {
        return 503;
    }
    error_page 503 /maintenance.html;
    location = /maintenance.html {
        root /var/www/bot-team/fred;
        internal;
    }

    # Proxy to gunicorn via unix socket
    location / {
        proxy_pass http://unix:/run/gunicorn-fred/gunicorn.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Block access to hidden files
    location ~ /\. {
        deny all;
    }
}
