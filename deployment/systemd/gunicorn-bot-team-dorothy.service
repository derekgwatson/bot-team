[Unit]
Description=Dorothy - Deployment Orchestrator Bot
After=network.target

[Service]
Type=notify
User=www-data
Group=www-data
RuntimeDirectory=gunicorn-dorothy
WorkingDirectory=/var/www/bot-team/dorothy
Environment="PATH=/var/www/bot-team/dorothy/.venv/bin:$PATH"
EnvironmentFile=/var/www/bot-team/dorothy/.env
ExecStart=/var/www/bot-team/dorothy/.venv/bin/gunicorn \
  --workers 2 \
  --worker-tmp-dir /dev/shm \
  --bind 0.0.0.0:8005 \
  --log-level debug \
  --capture-output \
  --access-logfile - \
  --error-logfile - \
  app:app
ExecReload=/bin/kill -s HUP $MAINPID
KillMode=mixed
TimeoutStopSec=30
PrivateTmp=true
Restart=on-failure
RestartSec=5
LimitNOFILE=4096

[Install]
WantedBy=multi-user.target
