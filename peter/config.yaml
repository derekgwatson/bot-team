# Peter's Configuration
name: peter
description: "Phone directory manager"
version: "0.1.0"

# Server configuration
server:
  host: "0.0.0.0"
  port: 8003

# Shared organization config
# See ../shared/config/organization.yaml for domain definitions
shared_config: "../shared/config/organization.yaml"

# Google Sheets configuration
google_sheets:
  # Path to service account credentials JSON file
  credentials_file: "credentials.json"
  # Spreadsheet ID (from URL)
  spreadsheet_id: "1ESrvIP52ThLQkOxpWcLWrPMeHcaerYQf6lHkUIC7DcA"
  # Sheet/tab name
  sheet_name: "Phone List"

# Authentication configuration
auth:
  # Google OAuth credentials - stored in .env file (not in git!)
  # See .env.example for required environment variables
  admin_emails:
    - "derek@watsonblinds.com.au"  # Only admins can manage phone directory
