# Quinn Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# and BOT_API_KEY are in the shared .env file at the root of bot-team/

# Flask secret key for sessions (generate a random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your-secret-key-here

# Allowed email addresses (comma-separated)
# Only these emails can access Quinn's admin interface
ALLOWED_EMAILS=your@email.com,another@email.com

# Google Groups - Allstaff Group Email
# The Google Group that Quinn manages for all staff members
GOOGLE_ALLSTAFF_GROUP=all-staff@example.com
