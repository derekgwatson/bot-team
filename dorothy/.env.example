# Dorothy's Configuration
# Copy this file to .env and fill in your values

# Note: Google OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET)
# are in the shared .env file at the root of bot-team/

# Sally's API URL (optional, defaults to http://localhost:8004)
# SALLY_URL=http://localhost:8004

# GitHub token for private repos (optional)
# GITHUB_TOKEN=your_token_here

# Flask secret key for sessions (generate with: python -c "import secrets; print(secrets.token_hex(32))")
FLASK_SECRET_KEY=your-secret-key-here

# Optional: Enable Flask debug mode (defaults to False for security)
# Set to "true" for development - shows detailed errors and auto-reloads
# FLASK_DEBUG=true

# Admin email addresses (comma-separated)
ADMIN_EMAILS=your@email.com,another@email.com

# Deployment Configuration
# -------------------------

# USE_SUDO: Whether to use sudo for system commands during deployment
# Set to "false" or "0" in environments where sudo is not available (e.g., containerized production)
# Set to "true" or "1" in environments where sudo is required (default)
# Default: true
# USE_SUDO=true
